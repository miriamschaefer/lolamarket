import React from 'react';

const Submenu = (props) => {

    // const categories = props.categories;
    // console.log(categories);


    // if (categories !== undefined) {
    //     categories.map((subcategory, i) => {
    //         return console.log(subcategory[i].categories)
    //     })

    // }


    return 'submenu'

}

export default Submenu;